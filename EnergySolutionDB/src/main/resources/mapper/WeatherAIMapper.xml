<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.energysolution.mapper.WeatherAIMapper">
	<insert id="insertWeatherData" parameterType="hashmap">
		INSERT INTO weatherData(Location, time, date, T1H, RN1, REH, WSD) VALUES (#{Location},#{time},#{date},#{T1H},#{RN1},#{REH},#{WSD})
	</insert>
</mapper>