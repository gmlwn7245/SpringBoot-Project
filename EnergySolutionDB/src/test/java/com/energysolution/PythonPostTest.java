package com.energysolution;

import org.junit.jupiter.api.Test;
import org.springframework.web.client.RestTemplate;
import org.springframework.web.util.UriComponents;
import org.springframework.web.util.UriComponentsBuilder;

public class PythonPostTest {
	
	private final String url = "http://192.168.1.80:5001";

	@Test
	public void test() {
//		RestTemplate template = new RestTemplate();
//		UriComponents uri = UriComponentsBuilder.fromHttpUrl(url+"/getAddress").queryParam("file", file).build(false);
//		String response = template.getForObject(uri.toString(), String.class);
//		System.out.println(response);
	}
}
